const chai = require('chai')
const expect = chai.expect()
const routes = require('../routes.recipe')

describe('Endpoint /recipes', function () {
  describe('GET recipes', function () {
    it('should return a JSON array of all recipes', function () {

    })
  })

  describe('POST recipe', function () {
    it('should ')
  })
})

describe('Endpoint /recipes/id', function () {
  it('should return a JSON ')
})
